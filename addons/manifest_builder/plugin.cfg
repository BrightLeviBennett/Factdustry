[plugin]

name="Manifest Builder"
description="Rebuilds data/game/manifest.json (natively, in-editor) whenever a registered .tres is saved, added, deleted, or renamed, so the runtime manifest stays in sync."
author="Factdustry"
version="2.0"
script="plugin.gd"
